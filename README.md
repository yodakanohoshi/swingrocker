# Jinja2 Markdown Template

このリポジトリには、Jinja2 を使った Markdown テンプレートと、テンプレートをレンダリングするサンプルスクリプト `main.py` が含まれます。

セットアップと実行方法（PowerShell）:

```powershell
python -m pip install -r requirements.txt
python main.py
```

実行すると、カレントディレクトリに `output.md` が生成されます。テンプレートは `templates/template.md.j2` にあります。
